# 修改记录

## 2026-06-01

### 5. 图片本地上传（含批量上传）
- **问题**：工具栏 🖼️ 按钮仅插入 `![描述](https://)` 文本模板，用户需手动填写 URL，无法直接从本地选择图片
- **解决方案**：
  - 新增隐藏的图片文件选择器（`<input type="file" accept="image/*" multiple>`）
  - 点击工具栏 🖼️ 按钮直接弹出系统文件选择器
  - 选中图片后自动存入 `ImageStore`（Blob URL + 短 ID），在光标处插入 `![文件名](img://xxx)` 引用
  - **批量上传**：文件选择器支持多选，一次可批量插入多张图片
  - 与现有拖拽上传共用同一套 `ImageStore` 存储和 `resolveMarkdown` 预览机制

### 文件变更
- `Claude排版工具 v3.html`：新增 `image-file-input` 元素、`handleImageFileSelect()` 函数，修改 `insertMarkdown('image')` 行为
- `CHANGELOG.md`：更新修改记录，版本号升级至 v3.1

---

## 2026-05-29

### 1. 快捷格式化工具栏 Tooltip 优化
- **问题**：鼠标悬停工具栏按钮时，tooltip 弹出时间过长（约 500ms-1s）
- **解决方案**：
  - 实现自定义 Tooltip 系统，使用 `data-title` 属性替代原生 `title`
  - 弹出延迟缩短至 **150ms**
  - 添加小三角箭头，提升视觉精致度
  - 包含悬停微动画效果

### 2. 图片拖拽上传功能
- **问题**：插入本地图片需要手动输入冗长的路径或 base64 数据，严重影响编辑体验
- **解决方案**：
  - 实现拖拽上传：从桌面/访达拖拽图片到编辑器区域
  - **存储优化**：使用 Blob URL + 短 ID 映射（`img://img_abc123`），编辑器内不再显示冗长 base64
  - **实时预览**：自动解析 `img://` 引用为 Blob URL，右侧预览正常显示
  - **批量支持**：支持多张图片同时拖拽，依次插入
  - **视觉反馈**：拖拽时编辑器边框高亮，placeholder 提示变化

### 3. 自动备份与审查机制
- **新增文件**：`lint.js`（自动审查脚本）
- **功能**：
  - 每次修改前自动创建备份（`Claude排版工具 v3.html.backup`）
  - 修改后自动运行 22 项检查（HTML 结构、关键函数、语法、新功能实现等）
  - **审查通过**：确认修改，保留备份
  - **审查失败**：自动回滚到备份版本，避免破坏主文件
  - **备份策略**：遵循"保留上一版本"原则，新备份覆盖旧备份

### 4. 导出逻辑优化
- **问题**：Blob URL 无法直接粘贴到微信公众号
- **解决方案**：
  - 复制 HTML 时，自动将所有 Blob URL 异步转换为 base64
  - 使用 DOMParser 解析 HTML，批量转换图片
  - 转换完成后生成完整的微信兼容 HTML
  - 编辑器内始终保持清爽的短引用格式

### 技术实现要点
- **ImageStore 对象**：管理 Blob URL 生命周期，支持存储、解析、清理
- **resolveMarkdown()**：将 `img://id` 实时解析为 Blob URL 供预览
- **convertBlobUrlsToBase64()**：异步转换，支持并发处理多张图片
- **拖拽事件处理**：完整的 dragenter/dragover/dragleave/drop 事件链

### 文件变更
- `Claude排版工具 v3.html`：主要修改文件
- `lint.js`：**新增**自动审查脚本
- `Claude排版工具 v3.html.backup`：自动生成的备份文件

---

*记录时间：2026-05-29*
*修改人：AI Assistant*